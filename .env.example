# Database (SQLite default)
DATABASE_URL="file:./prisma/homestead.db"

# App
NEXT_PUBLIC_APP_NAME="HomesteadHub"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Optional: Server-side secrets (if any added later)
# NODE_ENV="development"
